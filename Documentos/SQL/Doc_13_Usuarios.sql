use BaseMunicipios;

#tabla Municipio
create table usuarios(id int primary key not null, 
 usuario varchar(20), 
 passwordd varchar(20)
);

INSERT INTO usuarios (id, usuario, passwordd) VALUES 
('1',	'tesjo', '12345');